/**
 * **DESCRIPTION:**
 * 
 * With this interface we can represent the return of the
 * custom hook of the useAppNavigator, that this custom
 * hook will return only one property that an object of this
 * type.
 */
interface UseAppNavigatorReturn {

}

/** * **DESCRIPTION:**
 * 
 * This interface currently have 0 properties but if you add any property
 * you must document it and type it correctly for the usability of the custom hook.
 */
interface UseAppNavigatorProps {

}

/**
 * **DESCRIPTION:**
 * 
 * You need to write a description of what the useAppNavigator do on here.
 * 
 * **RETURNS:**
 * 
 * @returns returns an object with the properties of: 
 * * need to write it
 * 
 * **EXAMPLE OF USE:**
 * 
 * @example
 * const { } = useAppNavigator({}); 
 */
export const useAppNavigator = ({ }: UseAppNavigatorProps): UseAppNavigatorReturn => {

    return {

    }

}
